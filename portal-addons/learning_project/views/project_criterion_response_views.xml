<?xml version="1.0"?>
<odoo>
    <record id="project_criterion_response_view_form" model="ir.ui.view">
        <field name="name">project_criterion_response.form</field>
        <field name="model">project_criterion_response</field>
        <field name="arch" type="xml">
            <form string="Project Criterion Response">
                <sheet>
                    <group>
                        <field name="submission" string="Submission" editable="false"/>
                        <field name="criterion" string="Criterion" editable="false"/>
                        <field name="result" attrs="{'invisible': [('submission_result', '=', 'not_graded')]}" string="Result" widget="badge" decoration-success="result == 'passed'" decoration-danger="result == 'did_not_pass'" decoration-warning="result == 'incomplete'" />
                        <field name="result" attrs="{'invisible': [('submission_result', '!=', 'not_graded')]}" string="Result" />
                        <field name="submission_result" string="Submission Result" invisible="1"/>
                        <field name="feedback_components" string="Feedback" class="row">
                            <tree create="false" delete="false" editable="top">
                                <field class="col-1" name="name" string="Name" readonly="1" attrs="{'invisible': [('is_show', '=', False)]}" />
                                <field class="col-11" name="content" string="Content" attrs="{'invisible': [('is_show', '=', False)]}" />
                                <field name="criterion_response_result" string="Result" invisible="1" />
                                <field name="is_show" string="Is Show" invisible="1" />
                            </tree>
                        </field>
                    </group>
                    <notebook>
                        <page string="Preview">
                            <field name="feed_back" widget="html" string="Preview"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="project_criterion_response_action" model="ir.actions.act_window">
        <field name="name">Project Criterion Response</field>
        <field name="res_model">project_criterion_response</field>
        <field name="view_mode">form</field>
    </record>
</odoo>
